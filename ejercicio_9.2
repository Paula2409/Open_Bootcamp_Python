from functools import reduce

numero = [1,2,3,4,5,6,7,8,9]


def filtrar_sumar_datos(numero):
    lista = list(filter(lambda a: a % 2 == 0, numero))
    print(lista)
    suma = reduce((lambda a,b: a + b), lista)
    print(suma)

filtrar_sumar_datos(numero)
